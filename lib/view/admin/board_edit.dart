import 'package:flutter/material.dart';

class BoardEdit extends StatelessWidget {
  const BoardEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
