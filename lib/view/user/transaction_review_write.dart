import 'package:flutter/material.dart';

class TransactionReviewWrite extends StatelessWidget {
  const TransactionReviewWrite({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
