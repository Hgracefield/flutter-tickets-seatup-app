import 'package:flutter/material.dart';

class TransactionTicketsCheck extends StatelessWidget {
  const TransactionTicketsCheck({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
