import 'package:flutter/material.dart';

class TransactionCancel extends StatelessWidget {
  const TransactionCancel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
