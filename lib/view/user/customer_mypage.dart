import 'package:flutter/material.dart';

class CustomerMypage extends StatelessWidget {
  const CustomerMypage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
