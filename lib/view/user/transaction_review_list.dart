import 'package:flutter/material.dart';

class TransactionReviewList extends StatelessWidget {
  const TransactionReviewList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
