import 'package:flutter/material.dart';

class CustomerInfoUpdate extends StatelessWidget {
  const CustomerInfoUpdate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
