import 'package:flutter/material.dart';

class SellRegister extends StatelessWidget {
  const SellRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
